function createTitle(title) {
return `The ${title}`
  }
  console.log(createTitle("Storm's Awakening"));






  
module.exports = {
    createTitle,
  // buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}